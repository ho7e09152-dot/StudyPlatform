# 문서 추가 지침

- 제목과 설명은 한국어로 작성하고 파일명은 영문 kebab-case를 유지한다.
- 코드, 경로, API 계약 값, 환경변수, CLI, 제품명과 frontmatter key/status는 번역하지 않는다.
- 구현·OpenAPI·migration을 정본으로 확인하며 추정 내용을 현재 동작처럼 쓰지 않는다.
- 문서 상태와 갱신일을 보존한다. 특히 `status: draft` 법무 초안은 공개 금지 상태를 유지한다.
- 링크를 상대 경로로 유지하고 문서 이동 시 모든 참조를 함께 갱신한다.
